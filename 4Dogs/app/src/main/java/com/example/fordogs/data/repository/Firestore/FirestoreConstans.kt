package com.example.fordogs.data.repository.Firestore

class FirestoreConstans {
    companion object {
        const val USERS = "users"
        const val MENSAJE = "Usuario no encontrado"
        const val MENSAJE_USURIO_NO_CREADO = "Usuario no creado"
    }
}